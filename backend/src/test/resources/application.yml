spring:
  datasource:
    url: jdbc:h2:mem:exam_system;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-change-this-secret-to-32-chars
    expiration-minutes: 60
  admin:
    username: admin
    initial-password: Admin@123
  import:
    blockchain-pdf: /tmp/not-used.pdf
